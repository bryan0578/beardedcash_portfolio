import resource from './resource.schema';
import resourcePlaylist from './resource-playlist.schema';
import faqs from './faqs.schema';

const schemas = [resource, resourcePlaylist, faqs]

export default schemas;